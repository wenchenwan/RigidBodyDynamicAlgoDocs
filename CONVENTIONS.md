# 笔记撰写与更新约定

## 1. 每章笔记的固定结构

所有 `docs/chXX-*.md` 遵循同一套骨架（见 [`templates/chapter-note.md`](templates/chapter-note.md)）：

| 小节 | 作用 |
|---|---|
| **本章定位** | 一句话说清这章解决什么问题、在全书中承上启下的位置 |
| **前置依赖** | 读这章之前必须掌握的前面章节内容 |
| **核心概念** | 术语 + 定义 + 为什么需要它（不是抄定义，要写"为什么"） |
| **关键公式** | 带推导线索的公式清单，不只是结论 |
| **算法 / 伪代码** | 有算法的章节必写，格式统一 |
| **易错点与陷阱** | 第一遍读容易理解错的地方 |
| **与其他章的联系** | 双向链接，避免知识孤岛 |
| **我的理解** | ✍️ 读完后自己填，用自己的话复述 |
| **疑问与待办** | ❓ 读完后自己填，同步到 `notes/questions.md` |
| **与原文的出入** | 📌 发现笔记和原书不符时记在这里并修正正文 |

前 8 节是预填的框架，后 3 节（✍️ ❓ 📌）留给阅读时填写。

## 2. 数学记号

- 行内公式用 `$...$`，独立公式用 `$$...$$`（GitHub 原生支持 LaTeX 渲染）。
- 空间向量（6D）用小写粗体：$\mathbf{v}, \mathbf{a}, \mathbf{f}$
- 3D 向量用小写：$\omega, v_O, n_O$
- 矩阵/算子用大写：$I, X, S, H$
- 力空间的量统一带上标 `*`：$X^*,\ \times^*$
- 严格区分 $M^6$（运动空间）与 $F^6$（力空间）的量，不混用记号。

完整符号表见 [`reference/notation.md`](reference/notation.md)。

## 3. 术语处理

**中文为主 + 英文术语**：术语首次出现时写成 `中文 (English)`，之后可只用中文或只用英文，
但同一文件内保持一致。例：

> 空间速度 (spatial velocity) 是一个 6D 向量……后文中空间速度都记作 $\mathbf{v}$。

术语对照表见 [`reference/glossary.md`](reference/glossary.md)，新术语随读随加。

## 4. 伪代码风格

统一用原书的记号，便于对照：

- 下标 $i$ 表示 body / joint 编号，$\lambda(i)$ 表示 body $i$ 的父节点 (parent)
- ${}^{i}X_{\lambda(i)}$ 表示从 $\lambda(i)$ 坐标系到 $i$ 坐标系的运动向量变换
- 循环方向明确标注：`for i = 1 to N`（外推 outward）/ `for i = N to 1`（内推 inward）
- 每个算法卡片注明：**输入 / 输出 / 复杂度 / 用途**

## 5. Commit 规范

```
<type>(<scope>): <简短描述>
```

| type | 用途 |
|---|---|
| `feat` | 新增章节笔记或新文件 |
| `docs` | 补充/扩写已有笔记内容 |
| `fix` | 修正错误（公式错、理解错、与原书不符） |
| `refactor` | 调整结构、拆分合并文件、改链接 |
| `chore` | 更新进度表、模板、杂项 |

`scope` 用章节号或文件名，例：

```
docs(ch02): 补充 Plücker 坐标基向量的几何解释
fix(ch07): 修正 ABA 第二趟中 p^a 的表达式，原笔记漏了 I^a c_i 项
chore: 更新 README 阅读进度至 3/10
```

## 6. 持续更新流程

每次读完一章（或一节）：

1. 在对应 `docs/chXX-*.md` 里填「我的理解 / 疑问 / 与原文的出入」；
2. 修正预填框架中与原书不符的内容；
3. 新术语补进 `reference/glossary.md`，新符号补进 `reference/notation.md`；
4. 更新 `README.md` 进度表里的状态和「当前进度：X / 10」；
5. commit + push。

一次 commit 只做一件事，方便以后回看某个概念是什么时候想通的。
