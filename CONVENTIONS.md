# 笔记撰写与更新约定

## 1. 每章笔记的固定结构

所有 `docs/chXX-*.md` 遵循同一套骨架（见 [`templates/chapter-note.md`](templates/chapter-note.md)）：

| 小节 | 作用 |
|---|---|
| **原书节次对照** | 节号 + 原书英文标题 + 页码，便于直接对照 PDF 定位 |
| **本章定位** | 一句话说清这章解决什么问题、在全书中承上启下的位置 |
| **前置依赖** | 读这章之前必须掌握的前面章节内容 |
| **核心概念** | 术语 + 定义 + 为什么需要它（不是抄定义，要写"为什么"） |
| **关键公式** | 带推导线索的公式清单，不只是结论 |
| **算法 / 伪代码** | 有算法的章节必写，格式统一 |
| **易错点与陷阱** | 第一遍读容易理解错的地方 |
| **与其他章的联系** | 双向链接，避免知识孤岛 |
| **我的理解** | ✍️ 读完后自己填，用自己的话复述 |
| **疑问与待办** | ❓ 读完后自己填，同步到 `notes/questions.md` |
| **与原文的出入** | 📌 发现笔记和原书不符时记在这里并修正正文 |

前面各节已按原书 PDF 逐节核对撰写，后 3 节（✍️ ❓ 📌）留给阅读时填写。

**引用规范**：
- 引用原书原文用 `> *"..."*` 引用块，并注明是原书原话
- 标 🔑 的是**原书中特别关键的论断**
- 标 💡 的是**笔记补充**的解释或实践建议
- 标 ⚠️ 的是易错点
- 公式尽量标注原书编号，如 `(6.14)`

## 2. 数学记号

- 行内公式用 `$...$`，独立公式用 `$$...$$`：两个 `$$` 各占一行，块内不留空行
  （GitHub 原生支持 LaTeX 渲染）。
- 空间向量（6D）按原书写法**不加粗**：$v, a, f, p$；只有当它与同名的 3D 向量出现在同一段落时
  （主要是第 2 章）才戴帽子区分：$\hat v=[\omega;\ v_O]$、$\hat f=[n_O;\ f]$。
- 3D 向量用小写：$\omega, v_O, n_O$；单位矩阵写 $\mathbf 1$。
- 矩阵/算子用大写：$I, X, S, H$；复合刚体惯性 $I^c$，铰接体惯性 $I^A$，消元后的 $I^a$。
- 转置写 $^{\mathsf T}$；树拓扑：父节点 $\lambda(i)$、子节点集 $\mu(i)$、子树 $\nu(i)$、祖先集 $\kappa(i)$。
- 力空间的量统一带上标 `*`：$X^*,\ \times^*$
- 严格区分 $M^6$（运动空间）与 $F^6$（力空间）的量，不混用记号。

完整符号表见 [`reference/notation.md`](reference/notation.md)。

### 一个独立公式块里只能有一个 `\tag`

KaTeX 和 MathJax 都只接受**每个公式块一个** `\tag{}`，出现第二个会报
`Multiple \tag`，然后把**整块**渲染成红色错误文本 —— 表现就是"公式乱码"。

并排写两三个式子是本仓库的常用版式，此时把编号合并到**末尾那一个** `\tag` 里：

```
$$
I^c_i=\sum_{j\in\nu(i)}I_j
\qquad\qquad
I^c_i=I_i+\sum_{j\in\mu(i)}I^c_j\tag{6.12, 6.13}
$$
```

编号按**从左到右的出现顺序**写。另外 `\tag` 只能用在 `$$...$$` 里，
行内 `$...$` 用 `\tag` 同样报错。

## 3. 术语处理

**中文为主 + 英文术语**：术语首次出现时写成 `中文 (English)`，之后可只用中文或只用英文，
但同一文件内保持一致。例：

> 空间速度 (spatial velocity) 是一个 6D 向量……后文中空间速度都记作 $v$（与 3D 向量并存时记作 $\hat v$）。

术语对照表见 [`reference/glossary.md`](reference/glossary.md)，新术语随读随加。

## 4. 伪代码风格

统一用原书的记号，便于对照：

- 下标 $i$ 表示 body / joint 编号，$\lambda(i)$ 表示 body $i$ 的父节点 (parent)
- ${}^{i}X_{\lambda(i)}$ 表示从 $\lambda(i)$ 坐标系到 $i$ 坐标系的运动向量变换
- 循环方向明确标注：`for i = 1 to N`（外推 outward）/ `for i = N to 1`（内推 inward）
- 每个算法卡片注明：**输入 / 输出 / 复杂度 / 用途**

### 代码块一律用纯 ASCII

正文（LaTeX 公式）里照用原书记号；但围栏代码块里**只允许 ASCII**。
原因：`ⁱ` `⁽ⁱ⁾` `ᵀ` `₀` `⁻¹` `q̈` 这类上下标不在 Consolas /
Courier New 等常用等宽字体里，渲染时会逐字回退到别的字体、宽度不一致，
并排两栏的伪代码会整个错位（看起来就是乱码）。对照表：

| 原书记号 | 代码块写法 | 说明 |
| --- | --- | --- |
| ${}^{i}X_{\lambda(i)}$ | `X[i,lam(i)]` | `X[a,b]` = 从坐标系 `b` 到坐标系 `a` 的变换 |
| ${}^{\lambda(i)}X_i^{*}$ | `X[lam(i),i]^*` | `^*` 是力向量（对偶）变换 |
| ${}^{0}a_g$、${}^{i}a_0$ | `{0}a_g`、`{i}a_0` | 前置坐标系上标写成 `{frame}` |
| $\lambda(i)$、$\tau$、$\mu$、$\nu$ | `lam(i)`、`tau`、`mu`、`nu` | 希腊字母拼写出来 |
| $\dot q$、$\ddot q$ | `qd`、`qdd` | |
| $S_i^{\mathsf T}$、$D_i^{-1}$ | `S_i^T`、`D_i^-1` | |
| $\ne$、$\in$、$\times$、$\times^{*}$ | `!=`、`in`、`x`、`x*` | |
| 乘号 $\cdot$ | `*` | 与 `^*` 区分开 |

分隔线用 `---`，不用 `─`（东亚宽度不确定，会把并排两栏推歪）。
注释用 `<-` 而不是 `←`。示意图（树状图、目录树）不受此约束，
但请把示意图的围栏语言标成 `text`（即 ```` ```text ````），检查器对 `text` 与 `mermaid` 围栏不做 ASCII 检查。

### 提交前自检

```bash
python3 code/lint_docs.py      # 渲染规则：多 \tag、行内 \tag、空公式块、代码块非 ASCII、表格里的裸 |、失效链接
python3 code/verify_all.py     # 公式与算法的数值验证
```

`lint_docs.py` 无输出且退出码为 0 即通过。

## 5. Commit 规范

```
<type>(<scope>): <简短描述>
```

| type | 用途 |
|---|---|
| `feat` | 新增章节笔记或新文件 |
| `docs` | 补充/扩写已有笔记内容 |
| `fix` | 修正错误（公式错、理解错、与原书不符） |
| `refactor` | 调整结构、拆分合并文件、改链接 |
| `chore` | 更新进度表、模板、杂项 |

**改了公式先跑验证**：`python3 code/verify_all.py`（以及相应的 `verify_chXX.py`）；**改了 Markdown 先跑** `python3 code/lint_docs.py`。

`scope` 用章节号或文件名，例：

```
docs(ch02): 补充 Plücker 坐标基向量的几何解释
fix(ch07): 修正 ABA 第二趟中 p^a 的表达式，原笔记漏了 I^a c_i 项
chore: 更新 README 阅读进度至第 3 章
```

## 6. 持续更新流程

每次读完一章（或一节）：

1. 在对应 `docs/chXX-*.md` 里填「我的理解 / 疑问 / 与原文的出入」；
2. 修正预填框架中与原书不符的内容；
3. 新术语补进 `reference/glossary.md`，新符号补进 `reference/notation.md`；
4. 更新 `README.md` 进度表里的状态和「当前进度」一行；
5. commit + push。

一次 commit 只做一件事，方便以后回看某个概念是什么时候想通的。
