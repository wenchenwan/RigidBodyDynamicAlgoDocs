# 第 N 章 中文标题 (English Title)

> **一句话概括**：……
>
> 原书 pp.X–Y，共 M 节。

## 原书节次对照

| 节 | 原书标题 | 页 |
|---|---|---|
| N.1 | English Section Title | X |
| N.2 | …… | …… |

## 本章定位

……（这章解决什么问题？为什么放在这个位置？）

## 前置依赖

- 第 X 章 §X.X：……

## 核心概念

### 概念名 (English Term)

**是什么**：

**为什么需要**：

**注意**：

## 关键公式

$$
H\ddot q+C=\tau\tag{N.1}
$$

- 推导线索：
- 适用条件：

一个 `$$` 块只放一个 `\tag`；并排的式子把编号合并成 `\tag{N.1, N.2}`（见 `CONVENTIONS.md` §2）。

## 算法 / 伪代码

```
算法名 (输入 -> 输出)，复杂度 O(?)

for i = 1 to N_B                                    <- 外推
    v_i = X[i,lam(i)] * v_lam(i) + S_i * qd_i
for i = N_B to 1                                    <- 内推
    tau_i = S_i^T * f_i
```

围栏内只用 ASCII（对照表见 `CONVENTIONS.md` §4）；示意图的围栏语言标成 `text`。

## 易错点与陷阱

1. ……

## 与其他章的联系

- 依赖（<-）：
- 被用于（->）：

---

## ✍️ 我的理解

<!-- 读完后用自己的话复述，不要抄书 -->

## ❓ 疑问与待办

<!-- 同步一份到 notes/questions.md -->

- [ ]

## 📌 与原文的出入

<!-- 发现本笔记与原书不符时记录在此，并修正上面的正文 -->
